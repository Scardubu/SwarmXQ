# SwarmX V6

V6 adds the autonomous self-improvement overlay.

Use:

```bash
swarm evolve-layer --cycles 1
```

The layer observes the runtime, critiques the baseline, produces bounded mutations, validates them, and stages safe candidates under `~/.swarmx/evolution-layer/`.
