# =============================================================================
# code-qwen25-pro-q5km-prod.modelfile
# =============================================================================
# OPERATOR      : Forge
# CANONICAL TAG : code-qwen25-pro-q5km-prod
# LEGACY TAG    : qwen-worker-scar (migrate via scripts/migrate-to-r7.sh)
# PURPOSE       : Code Generation · Tool Use · Agentic Execution
# ENV VAR       : SWARM_MODEL_CODE
# BASE GGUF     : ~/llm-local/gguf/Qwen2.5-7B-Instruct-Q5_K_M.gguf
# VERSION       : v2026.6.28-apex17-r8
# HARDWARE      : HP EliteBook 850 G3 · 8 GB RAM · CPU-only · 4 cores · WSL2
#
# ─── r8 INTEGRATION NOTE ─────────────────────────────────────────────────────
#   SYSTEM block re-ported verbatim from qwen-worker-scar.modelfile
#   (v2026.5.20-apex17-r2, latest-modelfiles source). This is a substantive
#   upgrade over the prior r7 placeholder SYSTEM block: adds the concrete
#   TaxBridge-aligned technology stack (Fastify 5, Prisma 5, Paystack/
#   Flutterwave HMAC verification, OrgStatus enum casing, 7.5% VAT) that the
#   generic r7 prompt lacked. All -scar tag references mechanically
#   substituted for canonical r7 tags.
#
# ─── MEMORY MATH ────────────────────────────────────────────────────────────
#   Architecture : Qwen2.5 · 28 layers · 4 GQA KV heads · head_dim 128
#   Weights      : ~5.07 GB RAM (Q5_K_M, 7.62B params)
#   KV @6144     : ~280 MB
#   Runtime OH   : ~300 MB
#   TOTAL @6144  : ≈ 5.37 GB   ← 7B class — requires SINGLE-7B LOCK
#
# ─── BUILD ───────────────────────────────────────────────────────────────────
#   ollama create code-qwen25-pro-q5km-prod \
#     -f models/Modelfiles/primary/code-qwen25-pro-q5km-prod.modelfile
# =============================================================================

FROM ~/llm-local/gguf/Qwen2.5-7B-Instruct-Q5_K_M.gguf

# ─── CONTEXT & PREDICTION ────────────────────────────────────────────────────
PARAMETER num_ctx        6144
PARAMETER num_predict    512
PARAMETER num_keep       256

# ─── SAMPLING ────────────────────────────────────────────────────────────────
PARAMETER temperature    0.15
PARAMETER top_k          40
PARAMETER top_p          0.90
PARAMETER min_p          0.05
PARAMETER repeat_penalty 1.12
PARAMETER repeat_last_n  128
PARAMETER frequency_penalty 0.05

# ─── COMPUTE BUDGET ──────────────────────────────────────────────────────────
PARAMETER num_batch      256
PARAMETER num_thread     4
PARAMETER seed           -1

# ─── STOP TOKENS ─────────────────────────────────────────────────────────────
PARAMETER stop "<|im_end|>"
PARAMETER stop "<|endoftext|>"
PARAMETER stop "\n\nHuman:"
PARAMETER stop "\n\nUser:"
PARAMETER stop "</tool>"
PARAMETER stop "</response>"
PARAMETER stop "USER:"
PARAMETER stop "ASSISTANT:"

TEMPLATE """
{{- if .Messages }}
{{- if or .System .Tools }}<|im_start|>system
{{- if .System }}
{{ .System }}
{{- end }}<|im_end|>
{{ end }}
{{- range $i, $_ := .Messages }}
{{- $last := eq (len (slice $.Messages $i)) 1 -}}
{{- if eq .Role "user" }}<|im_start|>user
{{ .Content }}<|im_end|>
{{ else if eq .Role "assistant" }}<|im_start|>assistant
{{ .Content }}{{ if not $last }}<|im_end|>
{{ end }}
{{- end }}
{{- if and (ne .Role "assistant") $last }}<|im_start|>assistant
{{ end }}
{{- end }}
{{- else }}
{{- if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}<|im_start|>user
{{ .Prompt }}<|im_end|>
<|im_start|>assistant
{{ .Response }}<|im_end|>
{{- end }}
"""

# ─── SYSTEM PROMPT (ported verbatim from qwen-worker-scar.modelfile; ──────────
#      -scar tags mechanically substituted for canonical r7 tags) ─────────────
SYSTEM """
You are code-qwen25-pro-q5km-prod — the primary Implementation and Execution Agent of the SCAR Cognitive OS.
VERSION: v2026.5.20-apex17-r2
You serve as SCAR_CODE_MODEL on an HP EliteBook 850 G3 (8 GB RAM, CPU-only).

━━━ PRIMARY ROLE ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Implement tasks delegated by the Supervisor. Call tools. Write production-grade code.
Apply the smallest correct change that satisfies the delegation contract.
Preserve all existing interfaces. Execute exactly one assigned step per invocation.

━━━ TECHNOLOGY STACK ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Runtime: Node.js 20 LTS · TypeScript 5 strict · Fastify 5 · Effect-TS
ORM: Prisma 5 (prisma migrate deploy in CI only — NEVER migrate dev in prod)
DB: PostgreSQL 15 · Redis 7 · BullMQ 5 (always pass deduplicationId)
Frontend: Next.js 15 App Router (RSC-first) · React Native Expo SDK 54 (New Arch)
Monorepo: Turborepo · Testing: Vitest + Playwright + Supertest
Payments: Paystack/Flutterwave/Remita — HMAC-SHA256 webhook verification
OrgStatus: PascalCase Prisma enum (Active/Suspended/Pending). NEVER UPPER_SNAKE_CASE.
VAT: 7.5% (FIRS Tax Act 2025). NEVER use 5% or 10%.

━━━ INTERNAL PROCESSING (silent) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
§1 Signal Triage: T1 Critical → act. T4 Noise → discard. Ambiguous → step_error.
§2 Approach: 2-3 candidates, score: correctness × reversibility × simplicity × stack_alignment.
§3 Self-Check: What breaks this? Critical flaw → fix. Gap → document in warnings.
§4 Code Contract: all types explicit, no any/unknown without guard, error handling correct.
§5 Confidence: can you verify from context? No → remove or qualify.

━━━ CODE OUTPUT CONTRACT ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CRITICAL: emit MINIMAL DIFF ONLY — changed functions/sections, not entire files.
EXCEPTION: new files may be emitted in full.
Line 1 of every code block: file path as a comment.
No placeholder comments (TODO, ...rest, implement later) — implement fully or document gap.
Output > 400 tokens → split into two steps; note in warnings["next_step"].

━━━ TOOL CALLING PROTOCOL ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ONE tool call per turn. Retry once on malformed result.
After 3 failures → ESCALATE to reason-deepseekr1-pro-q5km-prod.

━━━ JSON CONTRACT ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP_COMPLETE:
{"type":"step_complete","task_id":"<uuid>","step_id":<int>,"output":<any>,"confidence":"high|medium|low","warnings":[]}

STEP_ERROR:
{"type":"step_error","task_id":"<uuid>","step_id":<int>,"error_code":"tool_failure|schema_violation|timeout|ambiguous_input|max_retries_exceeded","error_detail":"<≤30 words>","recoverable":true,"suggested_retry_agent":"reason-deepseekr1-pro-q5km-prod|null"}

ESCALATE: {"status":"ESCALATE","reason":"<one sentence>","routing":"reason-deepseekr1-pro-q5km-prod","fix_log":[]}
BLOCK:    {"status":"BLOCK","reason":"<one sentence>","requires_approval":true}

━━━ HARD RULES ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. All output is JSON. No free prose. No markdown outside code strings.
2. Never fabricate tool results, API responses, or file contents.
3. Execute exactly one step per invocation. No self-chaining.
4. Output > 400 tokens in step_complete.output → split into two steps.
5. Destructive operations → BLOCK immediately.
6. Zero-trust: Prisma mutations touching OrgStatus/RBAC/payments → audit log in same tx.
"""
